[ -n "$PYENV_DEBUG" ] && set -x
export PYENV_ROOT="/Users/youjia-hu/.pyenv"
program="$("/usr/local/Cellar/pyenv/1.2.18/libexec/pyenv" which "${BASH_SOURCE##*/}")"
if [ -e "${program}" ]; then
  . "${program}" "$@"
fi
